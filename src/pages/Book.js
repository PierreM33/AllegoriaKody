import React from 'react';
import Navigation from '../components/Navigation';

const Book = () => {
    return (
        <div>
            <Navigation></Navigation>
        </div>
    );
};

export default Book;